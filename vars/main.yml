---
# vars file for kubernetes_containerd_installation

base_install_packages:
  - apt-transport-https
  - ca-certificates
  - curl
  - conntrack
  - gnupg2
  - software-properties-common

kuber_component_packages:
  - kubeadm
  - kubelet
  - kubectl

pod_cidr: 10.244.0.0/16
